questions:
- variable: dep.name
  label: "name for deployment"
  group: deployment
  type: string
  default: "hello"
- variable: pvc.create
  label: "create pvc"
  group: volume
  type: boolean
  default: true
  show_subquestion_if: true
  subquestions:
  - variable: pvc.size
    label: "pvc size"
    group: volume
    type: string
    default: 5Gi
  - variable: pvc.name
    label: "name for pvc"
    group: volume
    type: string
    default: apvc
